overwrite: true
schema: 'src/graphql-schemas/*.graphql'
generates:
  src/graphql-schemas/types.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      enumsAsTypes: true
      contextType: ../graphql/graphqlContext#GraphqlContextType
      useTypeImports: true
