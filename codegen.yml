overwrite: true
schema: 'src/graphql-schemas/*.graphql'
generates:
  src/graphql-schemas/generated_graphql_types.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      useTypeImports: true
