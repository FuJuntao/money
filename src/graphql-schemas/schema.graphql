type Account {
  id: Int!
  name: String!
}

type Transaction {
  id: Int!
  from: Int
  to: Int
  amount: Int!
}

type Query {
  accounts: [Account]!
  transactions: [Transaction]!
}

input CreateAccountInput {
  name: String!
}

type Mutation {
  createAccount(input: CreateAccountInput!): Account
}
